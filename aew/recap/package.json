{
  "name": "fandom-top-contributors",
  "version": "1.0.0",
  "description": "A 'bot' that fetches data from a specified URL and posts it to a Discord channel via a webhook every week.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "node --loader ts-node/esm src/index.ts",
    "build": "tsc"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "license": "MIT"
}
